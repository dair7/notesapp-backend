package com.notesapp.enums;

public enum RoleType {
    USER,           // Usuario normal
    ADMIN,          // Administrador (creado por SUPER_ADMIN)
    SUPER_ADMIN     // Superadministrador (creado al arrancar el sistema)
}
